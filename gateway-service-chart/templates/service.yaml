apiVersion: v1
kind: Service
metadata:
  name: {{ include "gateway-service-chart.fullname" . }}
  labels:
    {{- include "gateway-service-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "gateway-service-chart.selectorLabels" . | nindent 4 }}
